# This app has no Vite-time (.env) configuration. All runtime config lives in
# public/config.js (window.config) — see public/config.js.example and
# src/config/windowConfig.ts. Copy that file instead:
#   cp public/config.js.example public/config.js
